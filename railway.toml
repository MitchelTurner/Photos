<<<<<<< HEAD
# Monorepo: Root Directory is set to /server so Nixpacks can detect
# Node.js correctly during the build image phase.
rootDirectory = "/server"

[build]
builder = "NIXPACKS"
buildCommand = "npm ci && npm run build"
=======
# Works with Root Directory = "/" (repo root).
# Prefer Root Directory = "server" + /server/railway.toml when possible.

[build]
builder = "NIXPACKS"
buildCommand = "npm run build"
>>>>>>> c67b03d (Fix Railway Nixpacks detecting nginx instead of Node)

[deploy]
startCommand = "npm run start:prod"
healthcheckPath = "/health"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
